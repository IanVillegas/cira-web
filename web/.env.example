# URL del backend (CIRA-Server). Por defecto apunta al servidor local.
# Durante la presentación, si exponés el puerto 3001 con Dev Tunnels de
# VS Code, poné acá la URL pública que te genera (ver CIRA-Server/README.md).
VITE_API_URL=http://localhost:3001
